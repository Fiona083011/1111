package comp1110.mse;

public class Q1Interval {
  /**
   * Return the smallest value in the array of values between index start and
   * index end (inclusive).
   *
   * Return zero if there are no values (because the values array is null or
   * empty, end is less than start, or the interval does not overlap the array).
   *
   * @param values an array of double values
   * @param start the first index in the interval
   * @param end the last index in the interval
   * @return the smallest value in the array of values between index start and
   * index end, or zero if the array is null or has no values, or there are no
   * values in the interval.
   */
  public static double interval(double[] values, int start, int end) {
    return -1.0; // FIXME Q1
  }
}
